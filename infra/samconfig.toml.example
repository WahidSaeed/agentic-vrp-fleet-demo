# Copy to samconfig.toml (gitignored) and adjust. No account-specific secrets here,
# but region / model id are environment-specific so the real file is not committed.
version = 0.1

[default.global.parameters]
stack_name = "fleet-demo"

[default.build.parameters]
cached = true
parallel = true

[default.deploy.parameters]
stack_name = "fleet-demo"
region = "eu-central-1"
capabilities = "CAPABILITY_IAM"
# Artifact bucket is a dedicated per-demo bucket that deploy.sh creates and
# teardown.sh removes (NOT the shared aws-sam-cli-managed-default stack).
# deploy.sh passes --s3-bucket / --s3-prefix explicitly.
confirm_changeset = false
disable_rollback = false
parameter_overrides = "DemoName=fleet-demo BedrockModelId=eu.anthropic.claude-haiku-4-5-20251001-v1:0"
tags = "Project=aws-community-day-demo Demo=fleet"

[default.delete.parameters]
stack_name = "fleet-demo"
region = "eu-central-1"
no_prompts = true
